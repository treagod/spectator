<schemalist>
    <schema id="com.github.treagod.httpinspector" path="/com/github/treagod/httpinspector/" gettext-domain="httpinspector">
        <key name="pos-x" type="i">
            <default>360</default>
            <summary>The saved horizontal position of the window.</summary>
            <description>The saved horizontal position of the window.</description>
        </key>

        <key name="pos-y" type="i">
            <default>360</default>
            <summary>The vertical position width of the window.</summary>
            <description>The saved vertical position of the window.</description>
        </key>

        <key name="window-width" type="i">
            <default>900</default>
            <summary>The saved width of the window.</summary>
            <description>The saved width of the window. Must be greater than 750, or it will not take effect.</description>
        </key>

        <key name="window-height" type="i">
            <default>600</default>
            <summary>The saved height of the window.</summary>
            <description>The saved height of the window. Must be greater than 500, or it will not take effect.</description>
        </key>

	    <key name="maximized" type="b">
            <default>false</default>
            <summary>Maximize the main window.</summary>
            <description>Wether the main window was maximized or not.</description>
        </key>

        <key name="follow-redirects" type="b">
            <default>true</default>
            <summary>Makes a new request to redirect.</summary>
            <description>Specifies if a new request should be made if the previous got a 301 - Redirect Header.</description>
        </key>

        <key name="use-proxy" type="b">
            <default>false</default>
            <summary>Use a proxy for every request.</summary>
            <description>Use a proxy for every request.</description>
        </key>

        <key name="http-proxy" type="s">
            <default>''</default>
            <summary>HTTP Proxy.</summary>
            <description>Specifies which HTTP Proxy to use.</description>
        </key>

        <key name="https-proxy" type="s">
            <default>''</default>
            <summary>HTTPS Proxy.</summary>
            <description>Specifies which HTTPS Proxy to use.</description>
        </key>

        <key name="no-proxy" type="s">
            <default>''</default>
            <summary>Defines No Proxy.</summary>
            <description>Specifies which domains should not be proxied.</description>
        </key>

        <key name="maximum-redirects" type="s">
            <default>'-1'</default>
            <summary>Maximum redirects.</summary>
            <description>Specifies how often a request can be redirected before the request is cancled.</description>
        </key>

        <key name="timeout" type="s">
            <default>'0'</default>
            <summary>Timeout.</summary>
            <description>Specifies how long a request should wait on a response before it's terminated.</description>
        </key>

        <key name="proxy-uri" type="s">
            <default>''</default>
            <summary>Proxy URI for the requests.</summary>
            <description>To which proxy the requests are sent.</description>
        </key>

        <key name="dark-theme" type="b">
            <default>false</default>
            <summary>Use dark theme</summary>
            <description>Switch between Light and Dark theme.</description>
        </key>
    </schema>
</schemalist>

